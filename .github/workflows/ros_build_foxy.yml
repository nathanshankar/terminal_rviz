name: ROS 2 CI Foxy

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: ros:foxy-ros-base-focal
    steps:
      - uses: actions/checkout@v4

      - name: Install dependencies
        shell: bash
        run: |
          apt update && apt install -y curl gnupg lsb-release
          curl https://packages.osrfoundation.org/gazebo.gpg --output /usr/share/keyrings/pkgs-osrf-archive-keyring.gpg
          echo "deb [arch=$(dpkg --print-architecture) signed-by=/usr/share/keyrings/pkgs-osrf-archive-keyring.gpg] http://packages.osrfoundation.org/gazebo/ubuntu-stable $(lsb_release -cs) main" | tee /etc/apt/sources.list.d/gazebo-stable.list > /dev/null
          apt update
          apt install -y build-essential cmake git libassimp-dev python3-colcon-common-extensions
          apt install -y ros-foxy-nav2-msgs ros-foxy-rclcpp-action ros-foxy-moveit-msgs ros-foxy-shape-msgs

      - name: rosdep install
        shell: bash
        run: |
          rosdep update
          rosdep install --from-paths . --ignore-src -r -y

      - name: build
        shell: bash
        run: |
          . /opt/ros/foxy/setup.sh
          colcon build --symlink-install --cmake-args -DCMAKE_BUILD_TYPE=Release
